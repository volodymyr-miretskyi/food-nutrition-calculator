export const AUTH_SERVICE_EVENTS = {
  LOGIN: 'auth.login',
} as const;

export const IMAGE_SERVICE_EVENTS = {
  UPLOAD: 'image.upload',
  GET_ALL: 'image.get-all',
} as const;

export const AI_SERVICE_EVENTS = {
  ANALYZE_IMAGE: 'ai.analyze-image',
} as const;
